
  # Teascape Prototype

  This is a code bundle for Teascape Prototype. The original project is available at https://www.figma.com/design/mrwQtP1LIqJuvbKIeaOI3J/Teascape-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  